# prepare the environment

poetry install

poetry shell

# run the backend api

python -m backend.app.main

# start the frontend

cd frontend

npm install

npm run dev

# result

view the app on port 3000
