{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "FastAPI backend for the agricultural advice application",
  "main": "app/main.py",
  "scripts": {
    "start": "python -m app.main",
    "dev": "uvicorn app.main:app --reload --port 8157"
  }
}