{
  "version": 2,
  "installCommand": "pnpm install --ignore-scripts",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "pnpm install --ignore-scripts",
        "buildCommand": "pnpm run build",
        "outputDirectory": "frontend/dist"
      }
    },
    {
      "src": "backend/app/main.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install poetry==1.7.1 && poetry install --no-dev --no-interaction --no-ansi --sync",
        "pythonVersion": "3.9"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/app/main.py"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
