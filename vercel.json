{
  "version": 2,
  "installCommand": "pnpm install --ignore-scripts",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "pnpm install --ignore-scripts",
        "buildCommand": "pnpm run build",
        "outputDirectory": "frontend/dist"
      }
    },
    {
      "src": "backend/app/main.py",
      "use": "@vercel/python",
      "config": {
        "installCommand": "pip install poetry==1.7.1 && poetry export -f requirements.txt --output requirements.txt --without-hashes --no-dev && echo '--- requirements.txt content ---' && cat requirements.txt && echo '--- installing from requirements.txt ---' && pip install -r requirements.txt --no-cache-dir && echo '--- pip freeze output ---' && pip freeze",
        "pythonVersion": "3.12"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/app/main.py"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
