{
  "name": "ag-advisor-frontend",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "lint": "eslint --ext .js,.vue,.ts --ignore-path ../.gitignore --fix src",
    "format": "prettier .  --write"
  },
  "dependencies": {
    "dompurify": "^3.2.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.6",
    "@types/dompurify": "^3.2.0"
  }
}
